const test=require('node:test'),assert=require('node:assert/strict'),C=require('../public/core.js');
test('bill splitting, discounts and markups use exact calculator arithmetic',()=>{for(const [q,value] of [['split 120 between 3','40'],['split 99 among 4 people','24.75'],['20% off 150','120'],['add 5% to 100','105'],['split ١٢٠ between ٣','40']]){assert.equal(C.calc(q).text,value);assert.equal(C.assistant(C.blank(),q).calculation,true)}assert.equal(C.assistant(C.blank(),'split 120 between 0').error,true)});
test('last month and category spending distinguish income, transfers and current month',()=>{let s=C.demo();const now=new Date(),prior=C.localDate(new Date(now.getFullYear(),now.getMonth()-1,1));for(const [kind,category,amount] of [['expense','Food & drink',1234],['expense','Groceries',456],['income','Salary',20000]])s=C.apply(s,{type:'transaction.add',kind,category,amount,account:s.accounts[0].id,date:prior});assert.match(C.assistant(s,'spending last month').text,/16.90/);assert.match(C.assistant(s,'salary last month').text,/200.00/);assert.match(C.assistant(s,'spending on food last month').text,/16.90/);assert.match(C.assistant(s,'spending on groceries last month').text,/4.56/);assert.match(C.assistant(s,'spending on bananas last month').text,/Choose a spending category/)});
test('budget replies handle unset, remaining and overspent limits without writes',()=>{let s=C.demo();assert.match(C.assistant(s,'budget').text,/No spending limit/);s=C.apply(s,{type:'settings',monthlyBudget:20000});const before=JSON.stringify(s);assert.match(C.assistant(s,'budget remaining').text,/Remaining AED 52.00/);assert.equal(JSON.stringify(s),before);s=C.apply(s,{type:'settings',monthlyBudget:10000});assert.match(C.assistant(s,'spending limit').text,/Over by AED 48.00/)});
