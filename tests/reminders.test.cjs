const test=require('node:test'),assert=require('node:assert/strict'),R=require('../public/reminders.js'),C=require('../public/core.js');
test('schedule deduplicates and sorts while rejecting invalid or excessive times',()=>{assert.deepEqual(R.times(['21:00','08:15','21:00']),['08:15','21:00']);for(const a of [['24:00'],['12:60'],['9:00'],Array(25).fill('09:00')])assert.throws(()=>R.times(a));assert.throws(()=>R.calendar([]))});
test('calendar exports one recurring local-time event and alarm per time',()=>{const text=R.calendar(['09:00','21:00'],new Date(2026,0,2,10,0));assert.equal((text.match(/BEGIN:VEVENT/g)||[]).length,2);assert.equal((text.match(/RRULE:FREQ=DAILY/g)||[]).length,2);assert.equal((text.match(/TRIGGER:PT0S/g)||[]).length,2);assert.match(text,/DTSTART:20260103T090000/);assert.match(text,/DTSTART:20260102T210000/);assert(text.endsWith('\r\n'));assert(!text.includes('AED'))});
test('schedules survive backup round trip and money reset without financial changes',()=>{let s=C.demo();const before=C.summary(s).total;s=C.apply(s,{type:'settings',reminderTimes:['20:30','09:00']});assert.equal(C.summary(s).total,before);assert.deepEqual(C.validate(JSON.parse(JSON.stringify(s))).settings.reminderTimes,['09:00','20:30']);assert.deepEqual(C.resetMoney(s).settings.reminderTimes,['09:00','20:30']);assert.throws(()=>C.apply(s,{type:'settings',reminderTimes:['bad']}))});
