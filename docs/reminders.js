/* Calendar export: importing/notifications are controlled by the user's calendar app. */
(function(root,factory){const value=factory();if(typeof module==='object'&&module.exports)module.exports=value;else root.DirhayaReminders=value})(globalThis,function(){'use strict';
function times(values){if(!Array.isArray(values)||values.length>24||values.some(v=>typeof v!=='string'||!/^([01]\d|2[0-3]):[0-5]\d$/.test(v)))throw Error('Choose up to 24 valid reminder times.');return [...new Set(values)].sort()}
const stamp=d=>`${d.getFullYear()}${String(d.getMonth()+1).padStart(2,'0')}${String(d.getDate()).padStart(2,'0')}T${String(d.getHours()).padStart(2,'0')}${String(d.getMinutes()).padStart(2,'0')}00`;
function calendar(values,now=new Date()){const schedule=times(values);if(!schedule.length)throw Error('Add at least one reminder time first.');const lines=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//Dirhaya//Daily reminders//EN','CALSCALE:GREGORIAN','X-WR-CALNAME:Dirhaya reminders'];for(const time of schedule){const [hour,minute]=time.split(':').map(Number),start=new Date(now);start.setHours(hour,minute,0,0);if(start<=now)start.setDate(start.getDate()+1);const end=new Date(start.getTime()+60000);lines.push('BEGIN:VEVENT','UID:dirhaya-daily-'+time.replace(':','')+'@dirhaya.local','DTSTAMP:'+now.toISOString().replace(/[-:]/g,'').replace(/\.\d{3}/,''),'DTSTART:'+stamp(start),'DTEND:'+stamp(end),'RRULE:FREQ=DAILY','SUMMARY:Log expenses in Dirhaya','DESCRIPTION:Open Dirhaya and record your recent expenses.','TRANSP:TRANSPARENT','BEGIN:VALARM','ACTION:DISPLAY','TRIGGER:PT0S','DESCRIPTION:Time to log your expenses','END:VALARM','END:VEVENT')}lines.push('END:VCALENDAR');return lines.join('\r\n')+'\r\n'}
return {times,calendar};});
